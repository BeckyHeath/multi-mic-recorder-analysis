Most TBC! 

from dead signal detection though. 

use the deadsigdetection.m script to generate the raw mean(abs) data + the corresponding text descriptions 

then used gainAdjust.m to bump the gain?

Maybe combine these?

